#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Validation basique du message de commit
# Le message doit contenir au moins 10 caractères
if [ ${#1} -lt 10 ]; then
  echo "❌ Message de commit trop court. Minimum 10 caractères requis."
  exit 1
fi

echo "✅ Message de commit valide"
