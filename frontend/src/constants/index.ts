export const Token = {
    KEYCHAIN_SERVICE_ACCESS: 'auth.accessToken',
    KEYCHAIN_SERVICE_REFRESH: 'auth.refreshToken'
}

export const Auth = {
    KEYCHAIN_SERVICE_CREDENTIALS: 'auth.credentials'
}